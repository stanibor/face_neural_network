import setuptools

setuptools.setup(
    name="face-neural-network",
    version="0.1.0",
    description="Architectures and datasets for face related NN training",
    packages=setuptools.find_packages(),
    python_requires='>=3.7',
)